import { QuotesDirective } from './quotes.directive';

describe('QuotesDirective', () => {
  it('should create an instance', () => {
    const directive = new QuotesDirective();
    expect(directive).toBeTruthy();
  });
});
